<bibliography>
  <layout>
    <text variable="citation-number" suffix=". "/>
    <choose>
      <if type="article-journal">
        <!-- formato para revistas -->
      </if>
      <else-if type="chapter">
        <!-- formato para capítulos de libro -->
      </else-if>
      <else-if type="webpage">
        <!-- formato para sitios web -->
      </else-if>
      <else>
        <!-- formato genérico -->
      </else>
    </choose>
  </layout>
</bibliography>
